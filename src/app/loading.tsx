import React, { ReactNode } from 'react'

const Loading = ({ children }: { children: ReactNode }) => {
  return (
    <div>
      <h1>Loading....</h1>
    </div>
  )
}

export default Loading

'use client'

import React from 'react'

import { CustomButton } from '@/components/Button'

const ActionButton = () => {
  return <CustomButton handleClick={() => {}} btnText='Edit' variant='contained' color='primary' />
}

export default ActionButton
